# .env.example — Copia este archivo como .env y completa los valores
# NUNCA subas el .env real al repositorio

# MySQL root (solo para el contenedor local)
DB_ROOT_PASSWORD=root_password_seguro

# Base de datos
DB_NAME=innovatech_db
DB_USERNAME=innovatech_user
DB_PASSWORD=innovatech_pass
